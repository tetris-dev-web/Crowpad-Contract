{
  "name": "hardhat-example",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.1",
    "hardhat": "^2.4.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.2.0",
    "@openzeppelin/test-helpers": "^0.5.15",
    "dotenv": "^10.0.0"
  },
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy-cronos-token.js --network development",
    "test": "npx hardhat --network development test test/test-cronos.js",
    "build": "npx hardhat compile",
    "deploy-cronos": "npx hardhat run scripts/deploy-cronos.js --network testnet",
    "test-cronos": "npx hardhat --network testnet test test/test-cronos.js"
  }
}
